# lightdash-el9

Prebuilt Lightdash artifacts (runtime tree + entrypoint + dumb-init) for AlmaLinux 9 / RHEL 9.

Releases are produced by `build/build-release.sh` in this repo and consumed
by `Dockerfile.runtime` on-prem.

See **[BUILD.md](BUILD.md)** for the runbook.
